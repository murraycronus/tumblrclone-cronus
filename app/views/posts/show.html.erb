
<div class="jumbotron">
    <div class="container">
        <div class="col-sm-8 col-sm-offset-4">
            <h2>
                <%= @post.title %>
            </h2>
            <small>
                Submitted <%= time_ago_in_words(@post.created_at) %>
            </small>
            <p>
                <%= @post.body %>
            </p>
        </div>
    </div>
</div>